export interface Wheel {
  name: string;
  series: string;
  imageUrl: string;
  slug: string;
}

export interface QuoteFormData {
  wheelName: string;
  name: string;
  email: string;
  phone: string;
  message: string;
}
